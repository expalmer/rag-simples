# RAG Simples

```bash
npm install
```

Para gerar os embeddings, execute o seguinte comando:

```bash
npm run update:embeddings
```

Para executar o agente, execute o seguinte comando:

```bash
npm start 'Quem usa sabre de luz verde ?'
```
